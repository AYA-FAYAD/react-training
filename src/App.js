function App() {
  // let massage = "Bye there!";
  // if (Math.random() > 0.5) {
  //   massage = "hello there";
  // }
  // return <h1>{massage}</h1>;

  return <input style={{ border: "3px solid red" }} type="number" min={5} />;
}

export default App;
